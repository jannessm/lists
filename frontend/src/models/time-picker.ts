import { Options } from "flatpickr/dist/types/options";

export const timePickerConfig: Options = {
    // altInput: false,
    enableTime: true,
    minuteIncrement: 5,
    disableMobile: false,
    time_24hr: true,
  };