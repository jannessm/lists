import { NameBadgePipe } from './name-badge.pipe';

describe('NameBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new NameBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
